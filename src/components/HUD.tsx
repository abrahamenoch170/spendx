import React from 'react';

export const HUD = ({ city, switchCity, layers, toggleLayer, ghostMode, setGhostMode, isFullMap, setIsFullMap, venueFilter, setVenueFilter }: any) => {
  return <div className="hud-placeholder">HUD</div>;
};
